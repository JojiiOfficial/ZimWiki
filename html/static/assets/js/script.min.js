$("#menu-toggle").click((function(e){e.preventDefault(),$("#wrapper").toggleClass("toggled")}));

function enterSearch(ele){
  if (event.key === 'Enter'){
    search(ele.value)
  }
}

function search(text) {
  window.location.href = "/search/"+wiki+"/?q="+text
}